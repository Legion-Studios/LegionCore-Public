// Airborne Base units
class ls_clone_airborne_rifleman: ls_clone_phase2_base {
    scope = 2;
    author = AUTHOR;
    displayName = "Rifleman (Phase 2)";
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_marksman: ls_clone_phase2_base {
    scope = 2;
    displayName = "Marksman (Phase 2)";
    ROLE_MARKSMAN
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_sniper: ls_clone_phase2_base {
    scope = 2;
    displayName = "Sniper (Phase 2)";
    ROLE_SNIPER
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_grenadier: ls_clone_phase2_base {
    scope = 2;
    displayName = "Grenadier (Phase 2)";
    ROLE_AT
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_medic: ls_clone_phase2_base {
    scope = 2;
    displayName = "Medic (Phase 2)";
    ROLE_MEDIC
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};
// Airborne Heroes?
