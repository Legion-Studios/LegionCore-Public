// Airborne Base units
class ls_clone_airborne_rifleman: ls_clone_phase2_base {
    scope = 2;
    author = AUTHOR;
    displayName = CSTRING(phase2_rifleman);
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_marksman: ls_clone_phase2_base {
    scope = 2;
    displayName = CSTRING(phase2_marksman);
    ROLE_MARKSMAN
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_sniper: ls_clone_phase2_base {
    scope = 2;
    displayName = CSTRING(phase2_sniper);
    ROLE_SNIPER
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_grenadier: ls_clone_phase2_base {
    scope = 2;
    displayName = CSTRING(phase2_grenadier);
    ROLE_AT
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};

class ls_clone_airborne_medic: ls_clone_phase2_base {
    scope = 2;
    displayName = CSTRING(phase2_medic);
    ROLE_MEDIC
    linkedItems[] += {
    };
    respawnLinkedItems[] += {
    };
};
// Airborne Heroes?
