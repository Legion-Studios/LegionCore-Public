class CfgEditorSubcategories {
    class ls_edsubcat_cis_standardDroids {
        displayName = CSTRING(battledroids);
    };
    class ls_edsubcat_cis_advancedDroids {
        displayName = CSTRING(battledroids_advanced);
    };
    class ls_edsubcat_cis_geonosisDroids {
        displayName = CSTRING(battledroids_geonosis);
    };
    class ls_edsubcat_cis_trainingDroids {
        displayName = CSTRING(battledroids_training);
    };
    class ls_edsubcat_cis_oomDroids {
        displayName = CSTRING(battledroids_oom);
    };
};
