#include "script_component.hpp"

#include "XEH_PREP.hpp"

// For ACE Arsenal category, here so items can be added to the category without the need to set `ls_equipment = 1` in config
uiNamespace setVariable [QGVAR(arsenalItems), []];
