class Civilian {
    // This class doesn't exist in vanilla, so we need to set these
    name = "$STR_Civilian";
    side = CIVILIAN;

    class ls_civilians {
        name = "[LS] Civilians";
    };
};
