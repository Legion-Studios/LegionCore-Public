class GVAR(base) {
    // Base class for settings, allows mods to inherit from this to automatically get default values
    enabled = 1;
    speed = 400;
    fuelDrain = 0.0001;
    overchargeSpeed = 600;
    overchargeFuelDrain = 0.0003;
    overchargeCooldown = 60;

    impulseSoundOn = "ls_impulseOn_laat";
    impulseSoundOff = "ls_impulseOff_laat";
    repulseSoundOn = "";
    repulseSoundOff = "";
};
