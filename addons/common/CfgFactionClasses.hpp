class CfgFactionClasses {
    class ls_gar {
        displayname = "[LS] Grand Army of the Republic";
        side = BLUFOR;
        icon = QPATHTOEF(data,insignias\republic_ca.paa);
        flag = QPATHTOEF(data,flags\flag_republic_ca.paa);
    };

    class ls_jedi {
        displayName = "[LS] Jedi Order";
        side = BLUFOR;
        icon = QPATHTOEF(data,insignias\jedi_ca.paa);
        flag = QPATHTOF(flags\flag_jedi_ca.paa);
    };

    class ls_csf {
        displayName = "[LS] Coruscant Security Force";
        side = BLUFOR;
    };

    class ls_orsf {
        displayName = "[LS] Outer Rim Security Forces";
        side = BLUFOR;
    };

    class ls_cis {
        displayname = "[LS] Confederacy of Independent Systems";
        side = OPFOR;
        icon = QPATHTOEF(data,insignias\cis_ca.paa);
        flag = QPATHTOEF(data,flags\flag_cis_ca.paa);
    };

    class ls_mandalorians {
        displayName = "[LS] Mandalorians";
        side = INDFOR;
        icon = QPATHTOEF(data,insignias\mandalorian_ca.paa);
        flag = QPATHTOF(flags\flag_mandalorian_ca.paa);
    };

    class ls_civilians {
        displayName = "[LS] Civilians";
        side = CIVILIAN;
    };

    class ls_osrf {
        displayName = "[LS] Orbital Security and Research Force";
        side = BLUFOR;
    };

    class ls_pirates {
        displayName = "[LS] Pirates";
        side = OPFOR;
    };

    class ls_mercenaries {
        displayName = "[LS] Mercenaries";
        side = INDFOR;
    };

    class ls_memefor {
        displayName = "[LS] Memefor";
        side = INDFOR;
    };

    class ls_jabiim {
        displayName = "[LS] Jabiim";
        side = INDFOR;
    };

    class ls_imperial {
        displayName = "[LS] Galactic Empire";
        side = OPFOR;
        icon = QPATHTOEF(data,insignias\imperial_ca.paa);
        flag = QPATHTOEF(data,flags\flag_imperial_ca.paa);
    };

    class ls_rebel {
        displayName = "[LS] Rebel Alliance";
        side = BLUFOR;
        icon = QPATHTOEF(data,insignias\rebelAlliance_ca.paa);
        flag = QPATHTOEF(data,flags\flag_rebelAlliance_ca.paa);
    };

    class ls_newRepublic {
        displayName = "[LS] New Republic";
        side = BLUFOR;
    };
};
