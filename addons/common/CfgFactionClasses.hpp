class CfgFactionClasses {
    class ls_gar {
        displayname = "[LS] Grand Army of the Republic";
        side = BLUFOR;
        icon = QPATHTOEF(data,insignias\republic_ca.paa);
        flag = QPATHTOEF(data,flags\flag_republic_ca.paa);
    };

    class ls_jedi {
        displayName = "[LS] Jedi Order";
        side = BLUFOR;
    };

    class ls_csf {
        displayName = "[LS] Coruscant Security Force";
        side = BLUFOR;
    };

    class ls_orsf {
        displayName = "[LS] Outer Rim Security Forces";
        side = BLUFOR;
    };

    class ls_cis {
        displayname = "[LS] Confederacy of Independent Systems";
        side = OPFOR;
        icon = QPATHTOEF(data,insignias\cis_ca.paa);
        flag = QPATHTOEF(data,flags\flag_cis_ca.paa);
    };

    class ls_mandalorians {
        displayName = "[LS] Mandalorians";
        side = INDFOR;
    };

    class ls_civilians {
        displayName = "[LS] Civilians";
        side = CIVILIAN;
    };

    class ls_osrf {
        displayName = "[LS] Orbital Security and Research Force";
        side = BLUFOR;
    };

    class ls_pirates {
        displayName = "[LS] Pirates";
        side = OPFOR;
    };

    class ls_mercenaries {
        displayName = "[LS] Mercenaries";
        side = INDFOR;
    };

    class ls_memefor {
        displayName = "[LS] Memefor";
        side = INDFOR;
    };

    class ls_jabiim {
        displayName = "[LS] Jabiim";
        side = INDFOR;
    };

    class ls_imperial {
        displayName = "[LS] Galactic Empire";
        side = OPFOR;
    };
};
