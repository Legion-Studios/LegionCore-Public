class CfgEditorSubcategories {
    class ls_edsubcat_special {
        displayName = "$STR_A3_firing_drills_special";
    };

    class ls_edsubcat_specialOperations {
        displayName = CSTRING(edsubcat_specialOperations);
    };

    class ls_edsubcat_nationalists {
        displayName = CSTRING(edsubcat_nationalists);
    };

    // Vehicles
    class ls_edsubcat_vehicles {
        displayName = "$STR_dn_vehicles";
    };

    class ls_edsubcat_wrecks {
        displayName = "$STR_A3_CFGVEHICLECLASSES_WRECK0";
    };

    class ls_edsubcat_speeders {
        displayName = CSTRING(edsubcat_speeders);
    };

    class ls_edsubcat_repulsorliftArmor {
        displayName = CSTRING(edsubcat_repulsorliftArmor);
    };

    class ls_edsubcat_walkers {
        displayName = CSTRING(edsubcat_walkers);
    };

    // Objects
    class ls_edsubcat_screenshotSupport {
        displayName = CSTRING(edsubcat_screenshotSupport);
    };

    class ls_edsubcat_staticShipParts {
        displayName = CSTRING(edsubcat_staticShipParts);
    };

    class ls_edsubcat_weapons {
        displayName = "$STR_A3_CfgEditorCategories_EdCat_Weapons0";
    };

    class ls_edsubcat_items {
        displayName = CSTRING(edsubcat_items);
    };

    class ls_edsubcat_itemsSmall {
        displayName = CSTRING(edsubcat_itemsSmall);
    };

    class ls_edsubcat_resupply {
        displayName = CSTRING(edsubcat_resupply);
    };

    class ls_edsubcat_tibanna {
        displayName = CSTRING(edsubcat_tibanna);
    };

    // Units
    class ls_edsubcat_droids {
        displayName = CSTRING(edsubcat_droids);
    };

    class ls_edsubcat_hallways {
        displayName = CSTRING(edsubcat_hallways);
    };

    class ls_edsubcat_fob {
        displayName = CSTRING(edsubcat_fob);
    };

    class ls_edsubcat_structures {
        displayName = "$STR_DN_STRUCTURES";
    };

    class ls_edsubcat_cloneTrooper_p1 {
        displayName = CSTRING(edsubcat_cloneTrooper_p1);
    };

    class ls_edsubcat_cloneTrooper_p2 {
        displayName = CSTRING(edsubcat_cloneTrooper_p2);
    };

    class ls_edsubcat_units {
        displayName = "$STR_disp_srvsetup_units";
    };

    class ls_edsubcat_chissAscendency {
        displayName = CSTRING(edsubcat_chissAscendency);
    };

    class ls_edsubcat_mirialan {
        displayName = CSTRING(edsubcat_mirialan);
    };

    class ls_edsubcat_zelosian {
        displayName = CSTRING(edsubcat_zelosian);
    };

    class ls_edsubcat_zeltron {
        displayName = CSTRING(edsubcat_zeltron);
    };

    class ls_edsubcat_zabrak {
        displayName = CSTRING(edsubcat_zabrak);
    };

    class ls_edsubcat_randomSpecies {
        displayName = CSTRING(edsubcat_randomSpecies);
    };
};
