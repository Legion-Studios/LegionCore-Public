class CfgFontFamilies {
    class ls_aurebesh {
        fonts[] = {FONT(aurebesh)};
    };

    class ls_aurebeshCondensed {
        fonts[] = {FONT(aurebeshCondensed)};
        spaceWidth = 0.6;
        spacing = 0.15;
    };

    class ls_republic {
        fonts[] = {FONT(republic)};
        spaceWidth = 0.6;
        spacing = 0.15;
    };

    class ls_scelent {
        fonts[] = {FONT(scelet)};
    };

    class ls_scelentAurebesh {
        fonts[] = {FONT(sceletAurebesh)};
    };
};
