class CfgEditorSubcategories {
    class ls_edsubcat_clone_5th {
        displayName = CSTRING(clone_5th);
    };
    class ls_edsubcat_clone_9th {
        displayName = CSTRING(clone_9th);
    };
    class ls_edsubcat_clone_13th {
        displayName = CSTRING(clone_13th);
    };
    class ls_edsubcat_clone_21st {
        displayName = CSTRING(clone_21st);
    };
    class ls_edsubcat_clone_34th {
        displayName = CSTRING(clone_34th);
    };
    class ls_edsubcat_clone_38th {
        displayName = CSTRING(clone_38th);
    };
    class ls_edsubcat_clone_41st {
        displayName = CSTRING(clone_41st);
    };
    class ls_edsubcat_clone_41stRb {
        displayName = CSTRING(clone_41stRb);
    };
    class ls_edsubcat_clone_41stgc {
        displayName = CSTRING(clone_41stgc);
    };
    class ls_edsubcat_clone_74th {
        displayName = CSTRING(clone_74th);
    };
    class ls_edsubcat_clone_91st {
        displayName = CSTRING(clone_91st);
    };
    class ls_edsubcat_clone_du {
        displayName = CSTRING(clone_du);
    };
    class ls_edsubcat_clone_104th {
        displayName = CSTRING(clone_104th);
    };
    class ls_edsubcat_clone_187th {
        displayName = CSTRING(clone_187th);
    };
    class ls_edsubcat_clone_212th {
        displayName = CSTRING(clone_212th);
    };
    class ls_edsubcat_clone_327th {
        displayName = CSTRING(clone_327th);
    };
    class ls_edsubcat_clone_332nd {
        displayName = CSTRING(clone_332nd);
    };
    class ls_edsubcat_clone_442nd {
        displayName = CSTRING(clone_442nd);
    };
    class ls_edsubcat_clone_501st {
        displayName = CSTRING(clone_501st);
    };
    class ls_edsubcat_clone_612th {
        displayName = CSTRING(clone_612th);
    };
    class ls_edsubcat_clone_coruscantGuard {
        displayName = CSTRING(clone_coruscantGuard);
    };
    class ls_edsubcat_clone_hornCompany {
        displayName = CSTRING(clone_hornCompany);
    };
    class ls_edsubcat_clone_kCompany {
        displayName = CSTRING(clone_kCompany);
    };
    class ls_edsubcat_clone_rancor {
        displayName = CSTRING(clone_rancor);
    };
    class ls_edsubcat_clone_misc {
        displayName = CSTRING(clone_misc);
    };
    class ls_edsubcat_clone_aiwhaSquad {
        displayName = CSTRING(clone_aiwhaSquad);
    };
    class ls_edsubcat_clone_bravoSquad {
        displayName = CSTRING(clone_bravoSquad);
    };
    class ls_edsubcat_clone_deltaSquad {
        displayName = CSTRING(clone_deltaSquad);
    };
    class ls_edsubcat_clone_omegaSquad {
        displayName = CSTRING(clone_omegaSquad);
    };
    class ls_edsubcat_clone_yayaxSquad {
        displayName = CSTRING(clone_yayaxSquad);
    };
    class ls_edsubcat_clone_mpSkins {
        displayName = CSTRING(clone_mpSkins);
    };
};
